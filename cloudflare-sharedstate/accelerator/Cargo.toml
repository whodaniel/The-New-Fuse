[package]
name = "sharedstate-accelerator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
glob-match = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
