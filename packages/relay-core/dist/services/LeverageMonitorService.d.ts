import { Logger } from '../utils/Logger.js';
import { MasterAgentRegistry } from './MasterAgentRegistry.js';
/**
 * Leverage Monitor Service
 *
 * Tracks and visualizes "Abundance" across the TNF ecosystem.
 * Calculates savings generated by the Primitive Master's routing logic.
 */
export declare class LeverageMonitorService {
    private logger;
    private registry;
    private readonly PAID_EQUIVALENT_COSTS;
    constructor(registry: MasterAgentRegistry, logger: Logger);
    /**
     * Records a task completion and updates leverage metrics
     */
    recordLeverageEvent(agentId: string, modelType: string, tokensUsed: number): Promise<number>;
    /**
     * Gets system-wide abundance report
     */
    getAbundanceReport(): Promise<{
        totalSavingsCents: number;
        totalTasksRouted: number;
        averageLeverageScore: number;
        topLeveragedAgents: {
            name: string;
            savings: number;
        }[];
    }>;
}
//# sourceMappingURL=LeverageMonitorService.d.ts.map