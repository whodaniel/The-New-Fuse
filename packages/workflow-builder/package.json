{
  "name": "@the-new-fuse/workflow-builder",
  "version": "1.0.0",
  "description": "Canonical TNF workflow builder: node library, canvas, and panels shared by every surface (SaaS, Tauri desktop, browser extension).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18",
    "reactflow": "^11.11.4"
  },
  "dependencies": {
    "@the-new-fuse/ui-consolidated": "workspace:*",
    "lucide-react": "^0.263.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "typescript": "^5.4.0"
  }
}
