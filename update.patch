--- apps/frontend/src/components/infinite-canvas.tsx
+++ apps/frontend/src/components/infinite-canvas.tsx
@@ -71,10 +71,10 @@
         ))}
       </framer_motion_1.motion.div>
       <div className="absolute top-4 left-4 space-x-2">
-        <button_1.Button onClick={() => handleZoom(1)}>
+        <button_1.Button onClick={() => handleZoom(1)} aria-label="Zoom in">
           <lucide_react_1.Plus className="h-4 w-4" />
         </button_1.Button>
-        <button_1.Button onClick={() => handleZoom(-1)}>
+        <button_1.Button onClick={() => handleZoom(-1)} aria-label="Zoom out">
           <lucide_react_1.Minus className="h-4 w-4" />
         </button_1.Button>
       </div>
--- apps/frontend/src/components/theme-toggle.tsx
+++ apps/frontend/src/components/theme-toggle.tsx
@@ -5,7 +5,7 @@
 export function ThemeToggle() {
   const { theme, toggleMode } = useTheme();
   return (
-    <Button variant="ghost" size="icon" onClick={toggleMode} className="w-9 h-9">
+    <Button variant="ghost" size="icon" onClick={toggleMode} className="w-9 h-9" aria-label="Toggle theme">
       <span className="sr-only">Toggle theme</span>
       {theme.mode === 'light' ? (
         <svg
--- apps/frontend/src/components/voice-controlled-commander.tsx
+++ apps/frontend/src/components/voice-controlled-commander.tsx
@@ -58,6 +58,7 @@
           variant={isListening ? 'destructive' : 'primary'}
           size="lg"
           className="rounded-full"
+          aria-label={isListening ? 'Stop voice command' : 'Start voice command'}
         >
           {isListening ? <MicOff className="w-6 h-6" /> : <Mic className="w-6 h-6" />}
         </Button>
