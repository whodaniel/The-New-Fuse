# 2026-08-30 — Departments, remember path, staffing index

- Operator can address TNF CLI by department: HR, Marketing, Design, Legal,
  Tech, Finance, Product, Ops (`tnf department list|show|route`).
- "Remember this" writes via `tnf remember retain` (harness layer +
  `~/.tnf/memory/notes.jsonl`). Recall is not enough.
- Agent/skill membership is in `data/departments/staffing-index.json`. Existing
  `category` values were not rewritten. Vendor skill packs indexed only.
- Progressive injection stays names → query → one body.
- Handoff multi-receipt Super Admin override:
  `TNF_HANDOFF_ALLOW_MULTI_RECEIPT=1` + matching Super Admin token. Do not use
  `--no-verify`.
- 45 `tnf-monorepo` back-burner PRs closed after cherry-picking unique CSPRNG /
  debounce / Turnstile fixes. Public `The-New-Fuse` PR 161 still open.
- Ops doc: `docs/operations/TNF_DEPARTMENTS_AND_MEMORY.md`.
- Docs/logging follow-through: LIVING_STATE, lexicon, FRONTLOAD Stage C, harness
  memory layer, SYSTEM_PROMPT, AGENTS.md, CLI README, Turn End receipt.
