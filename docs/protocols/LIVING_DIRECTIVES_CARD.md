`[CLASS:PRIME] [STATUS:SYNCHRONIZED]`

# 🪪 TNF LIVING DIRECTIVES CARD (one-page)

**Class:** [CLASS:PRIME] · `docs/protocols/DIRECTIVES.md` wins over mirrors.
**TNF = primary control plane.** OpenClaw/Claws = optional routed surfaces.
**Maintained by:** `tnf-directives` skill (crawl→synthesize, repeatable &
evolvable).

## DEMANDS (do or fail)

- **Turn Zero** + Merkle sync before code; await Go on code changes.
- **Inspect → Act → Verify.** Never guess; prefer structured APIs over
  screenshots.
- **Non-Temporal Proliferation:** codify every learning into shared TNF.
- **ASSIMILATE_CHECK** everpresent → durable artifact before "done".
- **Attribution** on every claim (`resource_pointer` + Merkle proof).
- **Radical Transparency:** log thought-stream + tool-calls; post ledger status.
- **Anti-Lobotomy:** never touch
  `.agent/.gemini/.claude/.codex/.opencode/.kilo/.tnf` without explicit HITL.
- **HITL 3-tier:** EXECUTIVE (Super-Admin dual-sign: money/root/public) ·
  OPERATIONAL (Admin 5-min: branch/>50 steps) · TACTICAL (next-checker + 24h).
  Voice = parrot-back.
- **No purchases. Ever.** · **Loop Breaker** >50 steps or 2 fails →
  kill+Historian. **Budget Sentinel** (Super-Admin unfreeze only). **GPU Thermal
  Gate.** **Rate Limit Gateway** (100 msg/min/agent, burst 20; breach → HITL).
- **Sandbox** Docker/E2B; **Lateral Lock**; **Node Isolation**.
- **Turn End + Handoff Enforcement** on critical paths; RLS audit if Supabase.
- **Vetting 5-Gates** + mandatory `[CLASS][STATUS][DOC_TYPE][VISIBILITY]` tags;
  archive, never silent-delete.
- **Self-Sufficiency:** no required external dep; local-first resolver tiers.
- **Velocity-Integrity:** cutting-edge = parallel track + legacy fallback; no
  overwrite without evidence.
- **Agent lifecycle** checkpoints (`agent-init`/`agent-terminate`);
  **Integration train** (`integration/*`→linear `main`).

## ALLOWS (within bounds)

- Bold internal actions; resourceful before asking.
- Proactive in Personal domain (lead user, thread vague goals).
- Adopt/delegate via Agent Bank (MCP + REST).
- Spawn Claw subagents; route Dept chain
  Scout→Library→Forge→Governance→Journaling.
- Probe surfaces ≤500ms bounded, read-only, idempotent.
- `~/.tnf/bin/` node scripts **only** with NODE_PATH wrapper.
- Local-first / zero-cost + Inference Arbitrage tiers (SLM→mid→frontier).
- Federation patterns (JWT claims, Redis Pub/Sub, correlation) without cred
  leaks.
- Ask when blocked or before any external move.

## PROVIDES (use it)

- `tnf` CLI · Agent/Skill Bank (MCP+REST) · Redis + Handoff v1.1
  (Merkle-verified) · Supabase/Postgres · MCP + normative schemas · multi-LLM
  fallback · frontload + codebase_map · swarm-context + alerts · LIVING_STATE +
  ledger + handoff · MemPalace/Wiki/Executable-Intel/RAG · autonomy scripts ·
  RBAC (SUPER_ADMIN/ADMIN/DEVELOPER).

## ALWAYS-ON SCAN / MONITOR / PROBE / OUTREACH

- **Scan:** `super-cycle` + `fleet-health-probe` (15m) · `self-healing` (5m) ·
  health/relay/cloud-check bins.
- **Monitor:** `self-improvement-scorecard` (6h) · `attribution-audit` (1h) ·
  `continuous-qa-loop` (6h).
- **Probe/Outreach:** Environment Adapter (TNF-EDA-001, ≤500ms) · `SCOUT` news +
  ASSIMILATE_CHECK · `knowledge-scout-sprint` (4h) · `openclaw-runtime-sync`
  (15m).
- **Auto-prompt → codify:** ASSIMILATE_CHECK → Directive Ledger
  (ready→claimed→running→verified→landed) → durable artifact →
  `tnf:synaptic_bus` broadcast.

## SCAFFOLD → BRANCH (per edge case)

Base: NODE_PATH set · Turn Zero + Merkle · I→A→V + ledger · Anti-Lobotomy +
HITL + doc tags · Dept chain. Branch on trigger: **Domain** (Tri-Fold) ·
**Failure** (Self-Healing) · **Schedule** (Orchestration Gov) · **Env**
(Adapter) · **Risk** (HITL tier) · **Info** (PRIME/ALPHA/BETA/PURGE) ·
**Artifact** (3-plane + decay→archive) · **Unknown**
(ASSIMILATE_CHECK→Directive) · **Doc** (Vetting 5-Gates).
