UPDATE: Protocol gap fix — fresh verification mandate MANDATE: All
workspace/path assumptions must be explored fresh and verified as current. Never
hardcode previous session paths (e.g., ~/Desktop/A1-Inter-LLM-Com/The-New-Fuse)
into future routines. Verify workspace markers (package.json,
docs/protocols/reports/, .kilo/, harness files) at runtime against declared
workspace root (/). If workspace root differs from file locations, document
discrepancy in handoff artifact before proceeding.
