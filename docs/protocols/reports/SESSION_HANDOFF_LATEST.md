TNF_PROTOCOL_ACK

# RC PHASE B T1 — AGENT MODULE RESOLUTION REPAIR (SCOPED RECEIPT)

Scoped receipt for T1 on `fix/rc-phase-b-test-integrity-20260828` (base = frozen
failed RC `f264e5e7d`). Covers the `@the-new-fuse/agent` jest module resolution
repair and the stale redis-agent-registry fixture rewrite.

Verification: individual test 4/4, full suite 13/13 natural exit 0, type-check
pass, build pass, emitted `.js` import suffixes preserved.

## Next Actions

- T2: mcp-core teardown leak diagnosis and lifecycle ownership repair
