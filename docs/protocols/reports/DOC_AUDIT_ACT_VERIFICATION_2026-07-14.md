# DOC AUDIT — Act Verification

`[CLASS:INTEL] [STATUS:VETTED]`

**Date:** 2026-07-14  
**Trigger:** Operator `Do all` (Act batches 1–3)

## Applied

| Batch | Actions                                                                                                                                                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | `REPO_SEPARATION` TL;DR → `The-New-Fuse`; open publish heading → `fuse-open-runtime`; `tauri-desktop`; orchestrator scripts under `scripts/registry/orchestrator/`; `sync-repos.sh` header; `local-oss-with-hosted-account`; lineage + key scripts slug sweep; `AGENTS.md` paths → `.agent/SOUL.md`, skills glob, `.agent/runtime-state.json` |
| 2     | License narrative unified to **MIT** in `README` + rewritten `GITHUB_README` (Node **20.20+**, TS **6.x**, no fantasy Agent/Harness API); **97** protocol files Gate-3 header backfill `[CLASS:*][STATUS:PENDING]` + machine headers on `DIRECTIVES`/`LIVING_DIRECTIVES_CARD`                                                                 |
| 3     | **39** package/app README stubs; `fairtable-adapters` name fix; `features/ui/command-palette` import → `@the-new-fuse/features/ui/command-palette`                                                                                                                                                                                            |

## Policy seals used

- Monorepo MIT end-to-end (root `LICENSE` wins)
- Node story = `.nvmrc` / `engines` (20.20+), not badge 22
- `the-new-fuse-next-gen` = historical 301 alias only
- `GITHUB_README` rewritten to match ground truth (P-B)

## Still open (non-blocking)

- Deep reclassify of 97 `STATUS:PENDING` protocol headers to LOCKED/VETTED via
  human/cron vetting
- Recreate missing `packages/control-plane-contracts` (called out in
  REPO_SEPARATION; not invented here)
- Skill corpus broken-path remediations (545 skills; sample only)
- Dirty tree commit from prior sessions still operator-owned
  (logs/turbo/artifacts)

## Verify

Re-run Tier A claim script after this Act — expect prior FAIL set cleared for
A1–A6 factual items.
