# L4+L5 Action Receipt — 2026-08-11

`[CLASS:INTEL] [STATUS:VERIFIED]`

Branch: `fix/l4l5-swarm-parity`

## Thin-client zombie dedup

| Metric                            | Before                        | After              |
| --------------------------------- | ----------------------------- | ------------------ |
| Redis `tnf:agent-registry` fields | ~381 (347 thin-client)        | 36 (1 thin-client) |
| Live `tnf list` thin-client rows  | hundreds / 1000+ historically | 1 stable id        |

Fixes:

- Stable Redis id for `tnf-thin-client` (`agent_<name>_<platform>`)
- Ephemeral cleanup uses `HDEL` instead of offline residue
- `tnf agents prune-stale` + `node scripts/tnf-agent-cli.cjs prune-stale`

## Hermes parity

| Metric                  | Before                  | After              |
| ----------------------- | ----------------------- | ------------------ |
| Hermes coverage         | 0% (options-only parse) | **100%** (0 gaps)  |
| Mean parity (8 tracked) | 38% / 190 gaps          | **52%** / 159 gaps |

Fixes:

- `parseHelpSurface` understands argparse `positional arguments:` + `{cmd,...}`
  braces
- Hermes root-option parity wrappers + remaining guide commands
  (`desktop`/`gui`/`journey`/`verify`)

## Verify commands

```bash
redis-cli HLEN tnf:agent-registry
node scripts/tnf-agent-cli.cjs prune-stale --name tnf-thin-client --dry-run --json
pnpm exec tsx packages/tnf-cli/src/cli.ts parity audit --agents hermes --json
pnpm exec tsx packages/tnf-cli/src/cli.ts parity audit
```
