# Release Readiness Summary 2026-06-19

Purpose: a one-page synthesis that can be shared with the operator without making them open the full checklist.

## Verdict (today)

TNF is **not** ready for full public release. It is ready for an opt-in alpha cohort that explicitly accepts documented gaps.

## What is proven clean (probed 2026-06-19T10:14Z)

- HTTPS, edge through Cloudflare, X-Frame-Options DENY, CSP set. (HSTS not in default response — one-click flip at the edge.)
- Frontend TTFB p95 ≈ 158 ms.
- Handoff-validation pipeline logs show zero `pre_gen_missing`.
- No leaked credentials in production tree.
- No `c2_heartbeat` threat pattern in the operational ledger.

## What is blocked

| ID  | Issue                                                                                                                                                                              |
|-----|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| M03 | `/health` JSON has no `timestamp` field ⇒ not monitorable from outside.                                                                                                            |
| M04 | ✅ RESOLVED — MCP placeholders replaced with real `@modelcontextprotocol/sdk` servers. Doctor section [3] now shows `OK (real MCP server)` for all three files. |
| M03 | `/health` JSON has no `timestamp` field ⇒ not monitorable from outside. |
| D02 | `data/mcp_config.json` invokes the placeholders above ⇒ MCP server fleet has only 3 of 6 real entries.                                                                             |
| M05 | Telegram daemon source exists but is not running; no public bridge health endpoint; WhatsApp pairing not verified.                                                                  |
| M02 | `/docs`, `/pricing`, `/features` return 404 (with JSON content type, *not* HTML stubs). Public marketing language refers to features that are not yet served at expected URLs.       |

## What is pending (laptop-bound probes need GCP/Cloud Run owner)

- V02 `/api/v1/health` endpoint absent.
- V03 signup flow 3-click / 15-second benchmark.
- P01–P03 SLOs (latency, error budget, recovery).
- R01–R02 supply-chain gate, rollback drill.
- D01 docs parity cron.
- S02 CORS endpoint-by-endpoint audit.

## Recommended posture

- **Alpha cohort private invite** with explicit disclosure of open gates above.
- Hold full marketing language until M02 + M03 + M04 + M05 close.
- Treat `tnf doctor` as a **doctor-of-record problem**: a check that returns green because of placeholder AST is worse than one that returns red, because it actively misleads future contributors and reviewers.

## Artifact path

- Checklist: `docs/release-readiness/CHECKLIST_V1_PUBLIC_RELEASE_READINESS.md`
- Evidence: `docs/release-readiness/evidence/*.md`
- Aggregated row table at bottom of checklist.

## Closing the path

Required order of operations:

1. ~~Replace M04 placeholders with transports (or delete and update doctor)~~ ✅ DONE (2026-06-19T15:30Z)
2. Add `timestamp` to `/health`.
3. Ship `/docs` (OpenAPI), `/pricing` (spec) endpoints as 200 JSON.
4. Implement `/bridges/{telegram,whatsapp}` health endpoints; pair WhatsApp.
5. CI-ize every probe in the M/V columns.
6. Flip checklist status to Release-Candidate; publish RELEASE_NOTES_PUBLIC.
