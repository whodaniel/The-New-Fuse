# Archived MCP root stubs (2026-08-09)

| File                   | Why archived                                                  |
| ---------------------- | ------------------------------------------------------------- |
| `claude-mcp-server.js` | ~20 LOC WS echo on :3712 — not MCP SDK / not shippable        |
| `gemini-mcp-server.js` | Demo Toolbox WS client on :3713 — not the Gemini product path |

Live MCP packages live in sibling directories under `apps/mcp-servers/`. See
`../README.md`.
