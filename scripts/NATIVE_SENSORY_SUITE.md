# TNF Native Sensory Suite (Forge Powered)

## Overview
The Native Sensory Suite provides high-performance, hardware-accelerated "senses" for the TNF Self-Synthesizing Kernel. By bypassing interpreted runtimes (Python/Node.js) and using the **TNF Forge** to synthesize JIT native modules, we achieve sub-millisecond latencies and high-fidelity sensory input.

## 🔭 Vision Bridge
High-speed screen mirroring and AI vision consumption.

- **Status:** ACTIVE
- **Relay URL:** `http://localhost:8080` (accessible on local network)
- **Primary Tool:** `scripts/tnf_remote_relay.py`
- **Native Modules:**
  - `bgra2jpeg_turbo.so`: SIMD-accelerated (AVX2/SSE2) JPEG encoding via libjpeg-turbo.
  - `screencap.so`: Zero-copy macOS screen capture via Quartz CoreGraphics.
- **Performance:**
  - ~34 FPS (29ms latency) on Intel i7 hardware.
  - Optimal Pipeline: MSS (Python) capture + `bgra2jpeg_turbo_native` (SIMD C).
- **Integration:** Exposed via `apps/mcp-servers/vision-bridge-mcp` (Tool: `capture_screen`).

## 👂 Audio Trigger
Sub-millisecond acoustic signature detection.

- **Status:** ACTIVE
- **Native Modules:**
  - `audio_spectral_matcher.so`: High-speed FFT engine.
- **Implementation:** C++ using macOS `Accelerate.framework` (vDSP) for hardware-accelerated spectral analysis.
- **Capabilities:**
  - Real-time PCM waveform matching.
  - Whistle/Tone trigger detection (e.g., 1000Hz tone detection verified).
  - <1ms latency for frequency peak analysis.
- **Test Script:** `scripts/audio_trigger_test.py`

## 🛠 Forge Evolution
The core Forge Compiler (`scripts/tnf_forge.py`) has been upgraded to support `extra_args`, enabling the kernel to link against system frameworks like **Accelerate**, **Metal**, and **CoreML** for future JIT hardware acceleration.

---
*Generated by Gemini CLI — May 8, 2026*
